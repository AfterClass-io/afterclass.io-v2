export enum ReviewableEnum {
  COURSE = "course",
  PROFESSOR = "professor",
}
export type ReviewableType = `${ReviewableEnum}`;

export enum SubmitAsEnum {
  ANONYMOUS = "anonymous",
  USER = "user",
}
